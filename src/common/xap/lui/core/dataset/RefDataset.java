package xap.lui.core.dataset;

import xap.lui.core.dataset.Dataset;
import xap.lui.core.dataset.IRefDataset;

public class RefDataset extends Dataset implements IRefDataset{

	public RefDataset(String id) {
		super(id);
	}

}
